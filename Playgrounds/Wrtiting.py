"""
Due to the complexity of the natural language, information on actions is not always named explicitly.
Therefore, readers have to infer implicit actions based on textual clues while explicit statements spell things out clearly.
Often, implict actions are formed by using the participle, as shown in the following example:

The extracting of information that is not explicitly stated in text is not always easy using rule-based approaches.
Therefore, we propose to use a language model to extract implicit actions from text.



"""